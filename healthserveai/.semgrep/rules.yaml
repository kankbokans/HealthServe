rules:
  - id: detect-hardcoded-google-api-key
    pattern-regex: 'AIzaSy[A-Za-z0-9_\-]*'
    message: "Potential hardcoded Google API key detected. Hardcoding credentials poses a severe security risk. Please use environment variables or a secure secret manager instead."
    languages:
      - python
    severity: ERROR
